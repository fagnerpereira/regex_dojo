<!DOCTYPE html>

<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Regex dojo</title>
    <%= favicon_tag %>
    <%= stylesheet_tag "app" %>
  </head>

  <body>
    <%= yield %>
    <script src="<%= asset_url("app.js") %>" nonce="<%= content_security_policy_nonce %>" type="text/javascript"></script>
  </body>
</html>
